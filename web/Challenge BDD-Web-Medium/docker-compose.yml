version: '3.8'

services:
  web:
    build: ./webapp
    ports:
      - "5001:5000"
    volumes:
      - ./webapp:/app
    environment:
      - FLASK_ENV=development
    restart: unless-stopped
